[pytest]
testpaths = tests
filterwarnings =
    # Starlette's own TestClient still imports httpx under a deprecation shim;
    # not our code, nothing actionable on our side.
    ignore:Using `httpx` with `starlette.testclient`:DeprecationWarning
    ignore:Using `httpx` with `starlette.testclient`
